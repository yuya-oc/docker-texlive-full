name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build latest --tag texlive-full:latest
    - name: Execute TeX
      run: docker run texlive-full:latest tex -version
